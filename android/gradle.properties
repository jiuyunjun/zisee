org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official

# 已部署的控制面。普通构建（含 Android Studio 的 Run）默认指向它；
# 本地联调用 -Pzisee.localBackend=true 覆盖为 adb reverse 的回环地址。
zisee.backendUrl=https://zisee-api-1042746204547.asia-northeast1.run.app
